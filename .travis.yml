language: php
sudo: false

php:
  - 7.3
  - 7.4

cache:
  directories:
    - "$HOME/.composer/cache"

matrix:
  fast_finish: true

install:
  - travis_retry composer install --no-scripts --no-suggest --no-interaction

script:
  - vendor/bin/phpunit